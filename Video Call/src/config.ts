export const config = {
  appId: "YOUR_AGORA_APP_ID", // Replace with your Agora app ID
  token: null, // For development, we'll use null. In production, you should use a token server
  channelName: "main"
};